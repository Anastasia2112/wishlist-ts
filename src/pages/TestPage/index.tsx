import { FC } from 'react';
import Loader from '../../components/Loader';

const TestPage: FC = () => {

  return (
    <section>
        <h1>Test page</h1>
        <Loader />
    </section>
  );
};

export default TestPage;
