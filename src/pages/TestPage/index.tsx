import { FC } from 'react';

import mock from '../../mock.json';

// import './styles.scss';

const TestPage: FC = () => {

  return (
    <section>
        Test page
    </section>
  );
};

export default TestPage;
